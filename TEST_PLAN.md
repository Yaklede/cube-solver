# 테스트 계획

## 자동 테스트

```bash
npm run test
```

포함된 테스트:

1. 큐브 상태 문자열 생성 테스트
2. 색상별 9개 검증 테스트
3. 잘못된 큐브 상태 감지 테스트
4. 회전 표기 파싱 테스트
5. 공식 적용 시뮬레이션 테스트
6. 학습 데이터 로딩 테스트
7. 공식 검색/필터 테스트
8. 사용자 진도 저장 테스트
9. 플랫폼별 빌드 스크립트 검증
10. 카메라 접근 실패 시 예외 처리 테스트

## 하네스 검증

```bash
npm run validate:harness
npm run lint:architecture
npm run validate:build-scripts
```

## 전체 내부 검증

```bash
npm run validate:all
```

## 수동 QA 체크리스트

- 웹앱이 `npm run dev`로 실행된다.
- 홈 화면에서 스캔/학습 이동이 가능하다.
- 카메라 권한 허용 시 프리뷰가 표시된다.
- 권한 거부 시 오류 메시지가 표시된다.
- 3x3 스캔 가이드가 중앙에 표시된다.
- 6면 탭이 U/R/F/D/L/B 순서로 동작한다.
- 팔레트 선택 후 스티커 수동 수정이 가능하다.
- 6면 저장 후 상태 문자열이 54자로 표시된다.
- 잘못된 상태에서 검증 오류가 표시된다.
- 풀이 안내에서 한 수 단위 이전/다음 이동이 가능하다.
- 학습 모드에서 왕초보/초보/CFOP/Roux/ZZ/Petrus가 표시된다.
- 공식 검색이 Sune, PLL, F2L 태그에 반응한다.
- 랜덤 테스트/기록 화면에서 성공/실패 기록이 누적된다.
- 모바일 폭에서 화면이 가로 스크롤 없이 동작한다.
- Tauri config가 macOS/Windows target을 포함한다.
