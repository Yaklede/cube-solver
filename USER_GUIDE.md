# 사용자 가이드

## 홈

홈 화면에서 `스캔 시작` 또는 `학습 열기`로 이동한다.

## 카메라 설정

카메라 권한을 허용한다. 브라우저가 카메라를 막으면 주소창 권한 설정에서 카메라를 허용한다.

## 색상 보정

초기 MVP는 기본 색상 프로필을 사용한다. 조명이 강하게 바뀌면 수동 수정 팔레트로 스티커 색상을 보정한다.

## 큐브 6면 스캔

1. U면부터 큐브를 가이드에 맞춘다.
2. 9칸 색상을 확인한다.
3. 잘못된 칸은 팔레트 색상을 선택한 뒤 칸을 누른다.
4. `현재 면 저장`을 누른다.
5. R/F/D/L/B 순서로 반복한다.

## 큐브 상태 수동 수정

각 스티커 버튼을 누르면 현재 선택한 팔레트 색상으로 변경된다. 중심 칸을 수정하면 해당 면의 중심 색상도 바뀐다.

## 풀이 안내

풀이 안내 화면에서 상태 문자열을 입력하고 `풀이 생성`을 누른다. 결과는 표준 회전 표기와 한국어 설명으로 표시된다. 3D 큐브는 현재 인식된 상태를 표시하고, `이전`, `다음`, `처음부터`로 진행할 때 같은 회전을 애니메이션으로 반영한다.

## 학습 모드

학습 모드는 과정, 레슨, 공식 라이브러리로 구성된다. 공식 검색창에서 케이스 이름, 태그, 회전 표기를 검색한다.

## 랜덤 테스트와 약점 복습

성공/실패 기록 버튼은 연습 결과 저장 흐름의 MVP이다. 실패가 성공보다 많으면 약점 공식으로 표시된다.

## 설정

Cross 색상, 느린 재생 속도, 회전 설명 표시 여부를 관리한다.
